{
  "name": "adaptaclass-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "postinstall": "node_modules/.bin/prisma generate --schema=backend/prisma/schema.prisma"
  }
}
